#include <stdbool.h>

bool funcao(int* A, size_t n, int i) {
	if (i >= n) {
		return false;
	}
	if (i >= 2) {
		int j = i-1, k = i-2;
		if (A[i] == A[j] + A[k])
			return true;
	}
	return funcao(A, n, i+1);
}

