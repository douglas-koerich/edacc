#ifndef ALG_BUSCA_H
#define ALG_BUSCA_H

#include "registro.h"

// Devolve o indice da posicao no vetor onde foi
// encontrada a chave passada como parametro (se nao, retorna -1)
int binaria(const Reg [*], int, int, unsigned);
int interpolacao(const Reg [*], int, int, unsigned);

#endif // ALG_BUSCA_H

